<!DOCTYPE html>
    <head>
        <meta charset="UTF-8">
        <title>preview</title>
    </head>
    <body>

    <?php

      echo( $presentType );
      echo( $title      );
      echo( $background );
      echo( $method     );
      echo( $result     );
      echo( $conclusions );
      

    ?>

    </body>
</html>